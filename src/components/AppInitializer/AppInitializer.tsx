'use client';

import { type FunctionComponent } from 'react';

const AppInitializer: FunctionComponent = () => {
  // @todo determine how this could be leveraged

  return null;
};

export default AppInitializer;
